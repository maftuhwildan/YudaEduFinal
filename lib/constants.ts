// Role constants have been consolidated into types/index.ts as enum Role.
// This file is kept for backwards compatibility — re-exports from types.
export { Role } from '@/types';
export type { RoleType } from '@/types';
